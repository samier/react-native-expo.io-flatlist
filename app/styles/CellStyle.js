import { StyleSheet } from 'react-native';
import Constants from '../constants/Constants';

/* Initialization of the StyleSheet of DogImageCell */
const styles = StyleSheet.create({
    cardViewContainer: {
        marginTop: 7,
        marginLeft: 7,
        marginRight: 7,
        marginBottom: 5,
        borderRadius: 5,
        height: 200,
        width: null,
        resizeMode: 'cover'
    },
});

export default styles;